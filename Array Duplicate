class Solution:
    def findDuplicates(self, arr):
        # code here
        a=[]
        se=set()
        sen=set()
    
        for i in arr:
            if i in sen :
                a.append(i)
                se.add(i)
            sen.add(i)
                
        
        return list(se)
