class Solution:
	def pushZerosToEnd(self, arr):
    	# code here
    	a1=[]
    	a2=[]
    	for i in range(len(arr)):
    	    if arr[i]==0:
    	        a1.append(0)
    	    else:
    	        a2.append(arr[i])
    	arr1=a2+a1
    	for j in range(len(arr1)):
    	    arr[j]=arr1[j]
